# 安全策略

## 支持范围

当前项目处于 `0.x` 早期预览阶段，只为最新的 `main` 分支和最新 GitHub Release 提供安全修复。旧版可能要求先升级后再复现。

## 私下报告漏洞

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能。如果该功能不可用，请联系仓库维护者，并在公开 Issue 中只说明“需要私下报告安全问题”，不要披露利用细节。

报告尽量包含：

- 受影响版本或提交；
- 漏洞类型和影响；
- 最小复现步骤或概念验证；
- 需要的本地文件、数据库权限或用户交互；
- 建议修复方式（如有）。

请在修复发布前给予维护者合理处理时间。维护者会尽力确认收到、评估影响并协调披露，但早期项目暂不承诺固定响应时限或漏洞奖励。

## 安全模型

- 数据工坊是本地桌面工具，不是多租户隔离平台。
- 外部插件是可信本地 Python 代码，不在沙箱中运行，并继承当前用户的文件和网络权限。
- 用户配置的 MySQL 查询、输出节点和项目存储初始化可访问或修改目标数据库。
- 打开恶意或超大文件可能消耗大量 CPU、内存和磁盘；只处理可信来源或在隔离环境中分析。
- CTF 解码和流量分析能力不构成授权；使用者必须遵守法律、比赛规则和数据所有者许可。

## 凭据与隐私

- 不要把数据库密码、令牌、私钥或真实项目文件提交到版本库。
- 生产环境使用专用最小权限数据库账号，不要使用 `root`。
- 对包含个人信息、业务日志或流量 Payload 的样本先脱敏，再附加到 Issue。
- `.gitignore` 已排除项目目录、缓存、构建产物和常见本地配置，但提交者仍应检查 `git diff --cached`。

## 不属于漏洞的情况

- 受信任外部插件执行任意本地代码；
- 用户主动使用高权限数据库账号后，配置的查询或输出修改数据；
- 无法证明安全影响的依赖版本扫描结果；
- 仅影响已停止支持的旧版本且最新版本无法复现的问题。
